models: # model directives, specifying the models to instantiate
  - blip2
postprocess_columns: # postprocessing directives
  - blip2-cap
additional_fields: # fields in a webdataset json to carry over into the metadata
  - hash #TODO: uid or hash
  - url
  - caption
  - original_width
  - original_height
  - sha256
nworkers: 2
batch_size: 512
device: 0
input_tars: "s3://s-laion/datanet_100M_pool-baselines/basic/all/seed_1/reshards_n1/0/" # brace expand suported
output_metadata_dir: "s3://s-laion/thaottn/100M_laion_coca_blip2_captions_temp_1.0" # can be arbitrary path - TODO: SET THIS
custom_pypath: "./custom/my_blip2.py" # if model not known, look in this python file for implementation
